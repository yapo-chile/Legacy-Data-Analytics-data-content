- defaultTab: nodes
  description: |-
        Testing process retention sellet packs inserting data in dm_analitycs in prod dwh database 
        <Add a description about what this ETL does>
        sudo docker run --rm -v /home/bnbiuser/secrets/dw_db:/app/db-secret \
                        -e APP_DB_SECRET=/app/db-secret \
                        containers.mpi-internal.com/yapo/retention-seller-pack:feat/retention-seller-pack
  executionEnabled: true
  group: CONTENT
  loglevel: DEBUG
  name: Content - Retention sellers packs
  nodeFilterEditable: false
  nodefilters:
    dispatch:
      excludePrecedence: true
      keepgoing: false
      rankOrder: ascending
      successOnEmptyNodeFilter: false
      threadcount: '1'
    filter: 'name: dockerhost'
  nodesSelectedByDefault: true
  notifyAvgDurationThreshold: null
  scheduleEnabled: false
  sequence:
    commands:
    - script: |-
        sudo docker pull containers.mpi-internal.com/yapo/retention-seller-pack:feat_retention-seller-pack
        sudo docker run --rm -v /home/bnbiuser/secrets/dw_db:/app/db-secret \
                        -e APP_DB_SECRET=/app/db-secret \
                        containers.mpi-internal.com/yapo/retention-seller-pack:feat_retention-seller-pack
    keepgoing: false
    strategy: node-first
  timeZone: America/Santiago
