# One .gitlab-ci.yml file for each project must be present.
# In order to run the docker-pipeline-template, a Dockerfile symlink must be placed
# into data-content/peak-new-approved-ads/Dockerfile (`ln -s ./app/dockerfile ./Dockerfile`).

"docker-push-hash:peak-new-approved-ads":
  extends: .docker-push-hash
  only:
    variables:
      - '$CI_COMMIT_BRANCH == "master" && ($CI_COMMIT_MESSAGE =~ /peak-new-approved-ads/ || $SUBFOLDER == "peak-new-approved-ads")'
  variables:
    CI_COMMIT_SHORT_SHA: "${CI_COMMIT_SHORT_SHA}_peak-new-approved-ads"
    IMAGE_TAG: "${CI_COMMIT_SHORT_SHA}"
  before_script:
    - cd ./peak-new-approved-ads/
    - sleep 5
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY

"deploy-rundeck:peak-new-approved-ads":
  extends: .deploy-rundeck
  dependencies:
    - docker-push-hash:peak-new-approved-ads
  when: manual
  only:
    variables:
      - '$CI_COMMIT_BRANCH == "master" && ($CI_COMMIT_MESSAGE =~ /peak-new-approved-ads/ || $SUBFOLDER == "peak-new-approved-ads")'
  variables:
    IMAGE_TAG: "${CI_COMMIT_SHORT_SHA}_peak-new-approved-ads"
    RUNDECK_MANIFEST: "deploy/rundeck.yaml"
    RUNDECK_PROJECT: "data_jobs"
  before_script:
    - apk add --no-cache curl gettext
    - cd ./peak-new-approved-ads/
