# One .gitlab-ci.yml file for each project must be present.
# In order to run the docker-pipeline-template, a Dockerfile symlink must be placed
# into data-content/peak-content-metrics/Dockerfile (`ln -s ./app/dockerfile ./Dockerfile`).

"docker-push-hash:peak-content-metrics":
  extends: .docker-push-hash
  only:
    variables:
      - '$CI_COMMIT_BRANCH == "master" && ($CI_COMMIT_MESSAGE =~ /peak-content-metrics/ || $SUBFOLDER == "peak-content-metrics")'
  variables:
    CI_COMMIT_SHORT_SHA: "${CI_COMMIT_SHORT_SHA}_peak-content-metrics"
    IMAGE_TAG: "${CI_COMMIT_SHORT_SHA}"
  before_script:
    - cd ./peak-content-metrics/
    - sleep 5
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY

"deploy-rundeck:peak-content-metrics":
  extends: .deploy-rundeck
  dependencies:
    - docker-push-hash:peak-content-metrics
  when: manual
  only:
    variables:
      - '$CI_COMMIT_BRANCH == "master" && ($CI_COMMIT_MESSAGE =~ /peak-content-metrics/ || $SUBFOLDER == "peak-content-metrics")'
  variables:
    IMAGE_TAG: "${CI_COMMIT_SHORT_SHA}_peak-content-metrics"
    RUNDECK_MANIFEST: "deploy/rundeck.yaml"
    RUNDECK_PROJECT: "data_jobs"
  before_script:
    - apk add --no-cache curl gettext
    - cd ./peak-content-metrics/
